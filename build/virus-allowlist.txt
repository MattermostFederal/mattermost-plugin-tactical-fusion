# ClamAV findings a release accepts: a path glob, then the signature it may
# match, one pair per line. Every other finding fails `make virus-scan`, and so
# does any scan error. Bundle archives are unpacked before scanning, so a glob
# names the file inside the bundle rather than the archive.
#
# Add an entry only for a false positive, with the reason and how it was found.
#
# MITRE ATT&CK's own text. attackdetail.tsv carries the procedure examples MITRE
# publishes, which quote real download and execution commands; this signature
# is assembled from parts that match across them. Bisected on 2026-09-25 to the
# rows T1027.017 through T1105 (Ingress Tool Transfer) of the committed file: no
# single row matches, only the span does.
*/assets/cyber/attackdetail.tsv  Win.Downloader.Agent-9865457-0
