bin/
dist/
coverage.txt
webapp/src/manifest.ts
server/manifest.go

# Mac
.DS_Store

# Jetbrains
.idea/

# VS Code
.vscode

# Claude Code local settings
.claude/settings.local.json

# Go workspace (local development)
go.work
go.work.sum

# Working notes
design-references/notes.md

# Docker development environment
docker/postgres-data/
docker/mattermost/

# CodeQL (downloaded during build)
build/codeql/
build/codeql-db/

# Spurious go.sum created by CodeQL in server directory
server/go.sum

# Playwright test output
test-results/

# Coverage reports
webapp/coverage/
webapp/coverage-ct/
webapp/coverage-merged/
webapp/.v8-ct-coverage/
webapp/.v8-unit-coverage/
webapp/.v8-merged-coverage/

# Built by webpack from webapp/src/page, like webapp/dist.
public/app/

# Downloaded Natural Earth sources and tiler intermediates for build/maptiles.
# The pinned digests in build/maptiles/sources.lock are what is committed; the
# 20MB of GeoJSON behind them is not, and neither are the per-tier archives.
build/maptiles/source/
build/maptiles/work/
